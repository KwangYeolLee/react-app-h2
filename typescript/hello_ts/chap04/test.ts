import sayHello from "./util.js"; // export default 를 import 하는 법
import { stringValue, numberValue } from "./util.js"; // export를 import 하는 법

sayHello("user");
console.log(stringValue, numberValue);
